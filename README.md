# 考試時程表系統

Node.js 自動備份版本

## 🚀 快速開始

### 安裝（首次使用）

1. **安裝 Node.js**
   - 下載：https://nodejs.org/（選擇 LTS 版本）
   - 安裝完成後，在專案資料夾開啟命令提示字元
   
2. **安裝依賴**
   ```bash
   npm install
   ```

### 使用

- **啟動**：雙擊 `open.bat`（背景運行，不顯示視窗）
- **停止**：雙擊 `stop.bat`
- **訪問**：http://localhost:8000

## ✨ 功能特色

- ⏰ **自動備份**：每週日晚上 23:00 自動備份所有數據
- 💾 **智能清理**：自動保留最近 2 個備份，刪除更舊的
- 🎯 **背景運行**：沒有擋路的視窗
- 📁 **備份位置**：`backups/` 資料夾
- 🔄 **手動備份**：仍可透過網頁界面手動備份和恢復

## 📂 檔案說明

- `server.js` - Node.js 伺服器（含自動備份功能）
- `open.bat` - 一鍵啟動（背景模式）
- `stop.bat` - 停止伺服器
- `start_hidden.vbs` - 隱藏視窗啟動腳本
- `backups/` - 自動備份資料夾

## ⚙️ 設定

### 修改備份時間

編輯 `server.js` 第 149 行：

```javascript
cron.schedule('0 23 * * 0', () => {
  // 每週日晚上 23:00
});
```

Cron 時間格式：`分 時 日 月 週`

### 修改保留備份數量

編輯 `server.js` 第 13 行：

```javascript
const MAX_BACKUPS = 2; // 改成想要的數量
```

## 📖 詳細文檔

請查看 `artifacts/nodejs_installation_guide.md` 獲取完整安裝和使用指南。

---

**Made with ❤️ for exam preparation**
